Add hint to varexpr to manually disambiguate things.
Allowed hints:
	fn
	type
	
If there is no hint, varexprs get looked up in the following order:

Name resolution for varexpr

-Always search for a variable. If we find one, that's the one.


Need a way to specify a particular overload of a function:

fn theThingImpl()
{
	print("hi");
}

fn theThingImpl(int i)
{
	print(i);
}

fn delegate (fn () theThing)
{
	theThing();
}

fn delegate (fn (int) theThing)
{
	theThing(4);
}

fn main()
{
	deletage(fn theThingImpl);		// Error, ambiguous argument
	delegate(fn theThingImpl());	// prints "hi"
	delegate(fn theThingImpl(int));	// prints "4";
}









Order of things

parse
	-all type definitions get put into type table
	-all references to types get put into pending list
		-types of vars
		-types of funcs		// should these 2 be separate? vars can be of func type...
		
resolve all pending types

resolve pass
	
