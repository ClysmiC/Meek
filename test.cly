// var printStuff = func(int x)() do print(x);

var computeFunc =
	func(int x)(float)
	{
		while true
		{
			x = x + 5;

			if x == 10 || x == 15 do
				continue;

			if x > 30 do
				break;
		}

		return sqrt(12 * x);
	};

float x = computeFunc(5);
