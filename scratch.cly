fn main(int a)
{
	int b = 5;
	b + a;

	// b + c;	// Should be an unresolve variable

	float f = 1.0;

	// b + f;	// Should be a type mismatch

	// float f = 3.0;	// Should be a variable redefinition
}